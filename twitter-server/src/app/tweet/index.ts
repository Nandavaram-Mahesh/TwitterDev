import {tweetTypes } from './types'
import {resolvers } from './resolvers'
import {tweetMutations } from './mutations'
import { queries } from './queries'

export const Tweet={tweetTypes,resolvers,tweetMutations,queries}